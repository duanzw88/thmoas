记录每章节的学习心得和每个接口可能的用处：
1.
2.
3.原子，原子是一个指针，指向一个唯一的、不可变的序列，序列中包含零个或多个字节
	原子具有唯一性，相同字符串在原子的存储中地址相同，
	优点：a 可以比较两个原子的指针即可比较两个字节序列是否相等
		 b 节省空间，因为任一序列都只会出现一次
	可能用处:使用任意序列作为索引，可以将原子用作键
11.序列
	序列包含N个值，分别关联到整数索引0到N-1(当N为正值时)。
	序列中的值可以通过索引访问，还可以从序列的两端添加或删除值。
	序列可根据需要自动扩展，以容纳其内容。
	序列中的值都是指针。
	尽管序列的规格相对简单，但是可以用作数组，链表，栈，队列和双端队列
	难理解点:
	1 seq_addlo，添加一个值到序列的开始，会将所有现存值的索引都加1，并将序列的长度加1
	  而seq_addhi，添加一个值到序列的末尾，只将序列的长度+1
	  同理
	  seq_remolo删除并返回seq序列起始处的值，会将余下所有值的序列索引都减1，并将序列的长度-1
	  seq_remohi删除并返回序列尾端的值，只将序列的长度-1

未完成想法:
1.利用ADT实现hashmap
2.利用ADT实现稀疏数组
3.稀疏矩阵压缩存储
